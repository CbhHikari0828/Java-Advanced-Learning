# Java 并发学习进度

最后更新：2026-08-04

这个文件用于在不同电脑之间同步学习上下文，也用于告诉新的 AI 助手：我现在学到哪里、接下来该怎么推进、应该用什么方式和我一起讨论 Java 并发。

## 1. 当前项目状态

- 仓库主题：Java 并发编程进阶学习。
- 当前阶段：`chapter05_atomic_and_volatile` 第一轮学习已完成，下一步进入 `chapter06_thread_pool`。
- 已完成：
  - 创建根目录 `README.md`，整理了整体学习路线。
  - 添加 `.gitignore`。
  - 初始化 Git 仓库，并推送到 GitHub `master` 分支。
  - 添加根目录 `pom.xml`，使用单模块 Maven 工程。
  - 添加 `src/main/java` 源码目录。
  - 确定源码目录规划：`src/main/java` 下使用 `chapter01_thread_foundation` 这类“编号 + 章节名”的文件夹放对应代码。
  - 初始化 `src/main/java` 下 10 个章节源码文件夹，并用 `package-info.java` 作为可编译占位。
  - 删除根目录下重复的旧章节文件夹，统一以 `src/main/java/chapterXX_...` 作为章节代码结构。
  - 完成 `chapter01_thread_foundation` 第一轮学习。
  - 编写并运行 `ThreadCreationDemo`、`CallableFutureDemo`、`ThreadStateDemo`。
  - 新增 `chapter01_thread_foundation/学习笔记.md`，记录核心理解。
  - 完成 `chapter02_synchronization` 第一轮学习。
  - 编写并运行 `RaceConditionDemo`、`LockObjectDemo`、`InstanceLockDemo`、`ReentrantSynchronizedDemo`、`VolatileVisibilityDemo`、`VolatileNotAtomicDemo`、`WaitNotifyDemo`。
  - 新增 `chapter02_synchronization/学习笔记.md`，记录 `synchronized`、锁对象、可重入、`volatile`、`wait/notify` 的核心理解。
  - 完成 `chapter03_lock_and_aqs` 第一轮学习。
  - 编写并运行 `ReentrantLockDemo`、`TryLockDemo`、`TimedTryLockDemo`、`ConditionDemo`。
  - 新增 `chapter03_lock_and_aqs/学习笔记.md`，记录 `ReentrantLock`、`tryLock`、`Condition`、公平锁和 AQS 的核心理解。
  - 完成 `chapter04_concurrent_collections` 第一轮学习。
  - 编写并运行 `ConcurrentHashMapDemo`、`PutIfAbsentDemo`、`ComputeIfAbsentDemo`、`SynchronizedMapDemo`、`MapIterationDemo`、`CopyOnWriteArrayListDemo`。
  - 新增 `chapter04_concurrent_collections/学习笔记.md`，记录 `HashMap` 与 `ConcurrentHashMap`、`putIfAbsent`、`computeIfAbsent`、`synchronizedMap`、并发遍历、`CopyOnWriteArrayList` 的核心理解。
- 完成 `chapter05_atomic_and_volatile` 第一轮学习。
- 编写并运行 `AtomicIntegerDemo`、`CompareAndSetDemo`、`CasCounterDemo`、`VolatileVisibilityDemo`、`VolatileCounterDemo`、`LongAdderDemo`。
- 新增 `chapter05_atomic_and_volatile/学习笔记.md`，记录原子性、`AtomicInteger`、CAS、`volatile` 和 `LongAdder` 的核心理解。
- 尚未完成：
  - 尚未添加测试目录。
  - 尚未开始 `chapter06_thread_pool`。

## 2. 我目前学到哪里

目前已完成 `chapter01_thread_foundation`、`chapter02_synchronization`、`chapter03_lock_and_aqs`、`chapter04_concurrent_collections` 和 `chapter05_atomic_and_volatile` 的第一轮学习，掌握了线程基础、任务模型、基础同步机制、显式锁、条件队列、AQS、常见并发容器、原子类、CAS、`volatile` 和高并发计数的基本直觉。

当前已经建立的直觉：

- `Thread` 是执行任务的线程。
- `Runnable` 是交给线程执行的任务。
- `Runnable` 没有返回结果，它的核心方法是 `void run()`。
- `start()` 才会启动新线程，直接调用 `run()` 只是普通方法调用。
- 如果需要从异步任务拿到结果，可以使用 `Callable + FutureTask`。
- `FutureTask.get()` 会等待任务完成并获取结果。
- 线程创建后但未启动是 `NEW`；调用 `start()` 后进入 `RUNNABLE`；调用 `sleep(...)` 会进入 `TIMED_WAITING`；`run()` 执行结束后进入 `TERMINATED`。
- 多个线程同时执行 `counter++` 会丢失更新，因为它不是原子操作。
- `synchronized` 保护临界区，让同一时间只有一个线程执行被保护代码。
- `synchronized` 代码块括号里写的是锁对象；实例同步方法锁 `this`，静态同步方法锁当前类的 `Class` 对象。
- `synchronized` 是可重入的，同一个线程可以再次进入同一把锁保护的代码。
- `volatile` 保证可见性，但不保证 `counter++` 这种复合操作的原子性。
- `wait()` 会释放锁并进入等待队列，`notify()` / `notifyAll()` 用来唤醒在同一个锁对象上等待的线程。
- `ReentrantLock` 是显式锁，需要手动 `lock()` 和 `unlock()`，标准写法是把 `unlock()` 放进 `finally`。
- `ReentrantLock` 是可重入锁，同一个线程可以多次获取同一把锁，但 `lock()` 几次就必须 `unlock()` 几次。
- `tryLock()` 拿不到锁时会立刻返回失败，不会一直等待。
- `tryLock(timeout)` 会最多等待一段时间，等不到再放弃。
- `Condition.await()` 会释放锁并进入等待队列，被 `signal()` / `signalAll()` 唤醒后还要重新竞争锁。
- `await()` 外面要用 `while` 反复检查条件，防止醒来后条件不成立。
- AQS 可以粗略理解成“状态变量 state + 等待队列”，负责管理锁状态、线程排队、阻塞和唤醒。
- 普通 `HashMap` 不适合多线程并发写入，可能出现数据丢失或遍历失败。
- `ConcurrentHashMap` 支持并发读写，适合多线程共享 Map 的场景。
- `Map.size()` 统计的是不同 key 的数量，多个线程写入相同 key 会覆盖旧值，不会增加 key 数量。
- `putIfAbsent` 把“检查是否存在”和“放入值”合成一个原子操作。
- `computeIfAbsent` 在 key 不存在时才执行计算逻辑，适合并发场景下的按需初始化。
- `Collections.synchronizedMap` 可以保护单次方法调用，但多个方法组合起来仍然不是天然原子的。
- `ConcurrentHashMap` 的遍历是弱一致性的：遍历安全，但不保证看到某一时刻的完整快照。
- `CopyOnWriteArrayList` 通过写时复制保证并发遍历安全，适合读多写少，不适合频繁写入。
- 普通 `int++` 包含读取、计算、写回三个步骤，不是原子操作，并发执行时会丢失更新。
- `AtomicInteger.incrementAndGet()` 能以原子方式完成自增；`AtomicInteger` 还支持 `get()`、`set()` 和 `compareAndSet()`。
- CAS 的含义是“比较并设置”：当前值等于期望值时才更新，否则失败并需要重新读取、重试。
- 普通 `int` 没有直接调用 CAS 的方法，初学阶段通过 `AtomicInteger` 使用 CAS。
- `volatile` 保证共享变量的可见性，但不保证 `counter++` 这类复合操作的原子性。
- `LongAdder` 适合高并发、频繁累加的统计场景，使用 `increment()` 累加、`sum()` 汇总；基础 Demo 只能证明并发安全，性能优势需要在高竞争对比实验中观察。
- 章节代码统一放在 `src/main/java/chapterXX_章节名` 下面，目录名本身就是合法 Java 包名。

接下来进入 `chapter06_thread_pool`，先学习线程池为什么能够复用线程，以及线程池如何管理任务。

1. 手动创建线程有什么问题。
2. `ExecutorService` 如何提交和执行任务。
3. 线程池中的线程如何复用。

## 3. 推荐下一步

建议下一次学习时先做这几件事：

1. 进入 `chapter06_thread_pool`。
2. 先写一个最小的 `ExecutorService` Demo，观察任务由线程池线程执行。
3. 再理解线程复用和 `shutdown()` 的作用。
4. 每个 Demo 保持短小，只说明一个知识点。
5. 每完成一个知识点，就在根目录 `README.md`、本文件和当前章节学习笔记里更新进度。

当前 Demo 进度：

- 已完成：`ThreadCreationDemo`：演示继承 `Thread`、实现 `Runnable`、`start()` 和 `run()` 的区别。
- 已完成：`CallableFutureDemo`：演示如何从异步任务中拿到返回值。
- 已完成：`ThreadStateDemo`：观察 `NEW`、`RUNNABLE`、`TIMED_WAITING`、`TERMINATED` 等线程状态。
- 已完成：`RaceConditionDemo`：演示多个线程同时修改共享变量时为什么会出错，以及如何用 `synchronized` 修复。
- 已完成：`LockObjectDemo`：演示同一锁对象会排队，不同锁对象互不阻塞。
- 已完成：`InstanceLockDemo`：演示实例锁和类锁的区别。
- 已完成：`ReentrantSynchronizedDemo`：演示 `synchronized` 可重入。
- 已完成：`VolatileVisibilityDemo`：演示 `volatile` 保证可见性。
- 已完成：`VolatileNotAtomicDemo`：演示 `volatile` 不保证原子性。
- 已完成：`WaitNotifyDemo`：演示 `wait / notify` 的线程协作流程。
- 已完成：`ReentrantLockDemo`：演示显式锁如何保护临界区、手动释放锁和可重入。
- 已完成：`TryLockDemo`：演示 `tryLock()` 拿不到锁时立刻放弃。
- 已完成：`TimedTryLockDemo`：演示 `tryLock(timeout)` 最多等待一段时间。
- 已完成：`ConditionDemo`：演示 `Condition.await()`、`signal()` 的线程协作流程。
- 已完成：`ConcurrentHashMapDemo`：演示普通 `HashMap` 并发写入不安全，以及 `ConcurrentHashMap` 的并发安全写入。
- 已完成：`PutIfAbsentDemo`：演示 `putIfAbsent` 的原子“没有才放入”。
- 已完成：`ComputeIfAbsentDemo`：演示 `computeIfAbsent` 的原子“没有才计算并放入”。
- 已完成：`SynchronizedMapDemo`：演示 `Collections.synchronizedMap` 对单次 Map 方法调用的同步保护。
- 已完成：`MapIterationDemo`：演示 `HashMap` fail-fast 和 `ConcurrentHashMap` 弱一致遍历。
- 已完成：`CopyOnWriteArrayListDemo`：演示 `CopyOnWriteArrayList` 的写时复制和并发遍历安全。
- 已完成：`AtomicIntegerDemo`：对比普通 `int++` 和 `AtomicInteger.incrementAndGet()`，观察并发丢失更新问题。
- 已完成：`CompareAndSetDemo`：演示 CAS 比较并设置的成功与失败。
- 已完成：`CasCounterDemo`：演示 CAS 失败后重新读取并重试，完成并发安全自增。
- 已完成：`VolatileVisibilityDemo`：演示 `volatile` 保证线程之间能看到共享变量的修改。
- 已完成：`VolatileCounterDemo`：演示 `volatile` 不保证 `counter++` 的原子性。
- 已完成：`LongAdderDemo`：演示 `LongAdder` 在并发计数中不会丢失更新。
- 下一步：`chapter06_thread_pool` 的线程池基础 Demo。

## 4. 和 AI 一起学习的风格

请 AI 按下面的方式和我协作：

- 用中文讲解，语气可以轻松一点，但不要跳步骤。
- 先问“这个知识点解决什么问题”，再写代码。
- 每次只推进一个小知识点，不要一次塞太多概念。
- 讲原理时要结合最小可运行 Demo。
- 代码要短，能复现现象即可，不追求复杂封装。
- 解释时多对比：为什么不用另一种写法，区别在哪里。
- 如果我概念混乱，优先用生活化类比帮我重新建立直觉。
- 不要只给结论，要引导我自己说出一部分理解。
- 默认不要直接替我生成或修改 Demo 代码，而是一步一步指导我自己写。
- 除非我明确要求“你来改文件 / 你来生成代码 / 直接改仓库”，否则 AI 不要操作源码文件。
- 如果需要长期记住协作方式，可以只更新本进度文件，方便换一个 AI 后继续保持同样节奏。

## 5. 每个知识点的讨论模板

学习每个知识点时，建议按这个顺序：

1. 它解决了什么问题？
2. 最小代码怎么写？
3. 运行结果说明了什么？
4. 背后的机制是什么？
5. 它可能在哪些场景出错？
6. 它和相近方案有什么区别？
7. 用一句话总结它的本质。

## 6. 模块推进顺序

当前建议按这个顺序推进：

1. `chapter01_thread_foundation`：线程创建、任务模型、线程状态。（已完成第一轮）
2. `chapter02_synchronization`：`synchronized`、`volatile`、`wait/notify`。（已完成第一轮）
3. `chapter03_lock_and_aqs`：`ReentrantLock`、`Condition`、AQS 基本思想。（已完成第一轮）
4. `chapter04_concurrent_collections`：并发容器的使用场景和取舍。（已完成第一轮）
5. `chapter05_atomic_and_volatile`：原子类、CAS、`LongAdder`、`volatile` 深入。（已完成第一轮）
6. `chapter06_thread_pool`：线程池参数、拒绝策略、任务调度。（下一步）
7. `chapter07_producer_consumer`：线程协作模型。
8. `chapter08_jmm_and_happens_before`：JMM、可见性、有序性、happens-before。
9. `chapter09_performance_and_debug`：死锁、线程 dump、JFR、VisualVM。
10. `chapter10_practice_projects`：综合练习。

## 7. 给下一位 AI 的提醒

- 当前项目不是一个完整 Java 工程，而是学习仓库骨架。
- 不要假设已经有构建工具或源码目录，先检查项目状态。
- 如果要继续推进，优先从 `chapter06_thread_pool` 的线程池基础 Demo 开始。
- 学习新 Demo 时，优先指导用户自己写代码，不要默认直接替用户新增或修改源码文件。
- 只有当用户明确要求 AI 直接改文件时，才修改源码；否则只提供分步骤指导、检查思路和解释。
- 每次用户完成一个 Demo 后，再根据用户要求更新根目录 `README.md`、本进度文件和当前章节学习笔记。
- 每次学习阶段结束，都要更新本文件的“我目前学到哪里”和“推荐下一步”。
- 保持示例代码简单、可运行、可解释。

