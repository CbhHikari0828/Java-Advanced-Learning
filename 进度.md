# Java 并发学习进度

最后更新：2026-07-29

这个文件用于在不同电脑之间同步学习上下文，也用于告诉新的 AI 助手：我现在学到哪里、接下来该怎么推进、应该用什么方式和我一起讨论 Java 并发。

## 1. 当前项目状态

- 仓库主题：Java 并发编程进阶学习。
- 当前阶段：项目骨架已搭好，尚未正式开始写 Java 示例代码。
- 已完成：
  - 创建根目录 `README.md`，整理了整体学习路线。
  - 创建 10 个学习模块目录。
  - 每个模块已有一个简短 `README.md`。
  - 添加 `.gitignore`。
  - 初始化 Git 仓库，并推送到 GitHub `master` 分支。
- 尚未完成：
  - 尚未初始化 Maven / Gradle 工程结构。
  - 尚未添加 `src/main/java` 或测试目录。
  - 尚未编写任何并发 Demo。

## 2. 我目前学到哪里

目前还处在“准备开始系统学习 Java 并发”的起点。

下一步应该从 `01-thread-foundation` 开始，而不是直接跳到线程池、AQS 或 JMM。优先理解：

1. `Thread` 是什么。
2. `Runnable` 和 `Thread` 的关系。
3. `Callable` / `Future` 解决了什么问题。
4. 线程生命周期和常见状态。

## 3. 推荐下一步

建议下一次学习时先做这几件事：

1. 初始化一个简单 Java 工程，优先使用 Maven。
2. 在 `01-thread-foundation` 下补充最小 Demo。
3. 每个 Demo 保持短小，只说明一个知识点。
4. 每完成一个知识点，就在对应模块 README 和本文件里更新进度。

第一个推荐 Demo：

- `ThreadCreationDemo`：演示继承 `Thread` 和实现 `Runnable` 的区别。
- `CallableFutureDemo`：演示如何从异步任务中拿到返回值。
- `ThreadStateDemo`：观察 `NEW`、`RUNNABLE`、`TIMED_WAITING`、`TERMINATED` 等线程状态。

## 4. 和 AI 一起学习的风格

请 AI 按下面的方式和我协作：

- 用中文讲解，语气可以轻松一点，但不要跳步骤。
- 先问“这个知识点解决什么问题”，再写代码。
- 每次只推进一个小知识点，不要一次塞太多概念。
- 讲原理时要结合最小可运行 Demo。
- 代码要短，能复现现象即可，不追求复杂封装。
- 解释时多对比：为什么不用另一种写法，区别在哪里。
- 如果我概念混乱，优先用生活化类比帮我重新建立直觉。
- 不要只给结论，要引导我自己说出一部分理解。
- 如果需要改仓库，请直接改文件，并说明改了哪里。

## 5. 每个知识点的讨论模板

学习每个知识点时，建议按这个顺序：

1. 它解决了什么问题？
2. 最小代码怎么写？
3. 运行结果说明了什么？
4. 背后的机制是什么？
5. 它可能在哪些场景出错？
6. 它和相近方案有什么区别？
7. 用一句话总结它的本质。

## 6. 模块推进顺序

当前建议按这个顺序推进：

1. `01-thread-foundation`：线程创建、任务模型、线程状态。
2. `02-synchronization`：`synchronized`、`volatile`、`wait/notify`。
3. `05-atomic-and-volatile`：原子类、CAS、`LongAdder`、`volatile` 深入。
4. `03-lock-and-aqs`：`ReentrantLock`、`Condition`、AQS 基本思想。
5. `04-concurrent-collections`：并发容器的使用场景和取舍。
6. `06-thread-pool`：线程池参数、拒绝策略、任务调度。
7. `07-producer-consumer`：线程协作模型。
8. `08-jmm-and-happens-before`：JMM、可见性、有序性、happens-before。
9. `09-performance-and-debug`：死锁、线程 dump、JFR、VisualVM。
10. `10-practice-projects`：综合练习。

## 7. 给下一位 AI 的提醒

- 当前项目不是一个完整 Java 工程，而是学习仓库骨架。
- 不要假设已经有构建工具或源码目录，先检查项目状态。
- 如果要继续推进，优先补 Maven 工程和 `01-thread-foundation` 示例。
- 每次新增 Demo，都要同时更新对应模块 README。
- 每次学习阶段结束，都要更新本文件的“我目前学到哪里”和“推荐下一步”。
- 保持示例代码简单、可运行、可解释。

