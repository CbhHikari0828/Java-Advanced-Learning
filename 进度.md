# Java 并发学习进度

最后更新：2026-07-30

这个文件用于在不同电脑之间同步学习上下文，也用于告诉新的 AI 助手：我现在学到哪里、接下来该怎么推进、应该用什么方式和我一起讨论 Java 并发。

## 1. 当前项目状态

- 仓库主题：Java 并发编程进阶学习。
- 当前阶段：已初始化 Maven 工程，并开始 `chapter01_thread_foundation` 的第一个线程基础 Demo。
- 已完成：
  - 创建根目录 `README.md`，整理了整体学习路线。
  - 添加 `.gitignore`。
  - 初始化 Git 仓库，并推送到 GitHub `master` 分支。
  - 添加根目录 `pom.xml`，使用单模块 Maven 工程。
  - 添加 `src/main/java` 源码目录。
  - 添加 `ThreadCreationDemo`，演示继承 `Thread` 和实现 `Runnable` 的区别。
  - 确定源码目录规划：`src/main/java` 下使用 `chapter01_thread_foundation` 这类“编号 + 章节名”的文件夹放对应代码。
  - 初始化 `src/main/java` 下 10 个章节源码文件夹，并用 `package-info.java` 作为可编译占位。
  - 删除根目录下重复的旧章节文件夹，统一以 `src/main/java/chapterXX_...` 作为章节代码结构。
- 尚未完成：
  - 尚未添加测试目录。
  - 尚未编写 `CallableFutureDemo`。
  - 尚未编写 `ThreadStateDemo`。

## 2. 我目前学到哪里

目前已经开始 `chapter01_thread_foundation`，第一个知识点是 `Thread` 和 `Runnable` 的关系。

当前已经建立的直觉：

- `Thread` 是执行任务的线程。
- `Runnable` 是交给线程执行的任务。
- `Runnable` 没有返回结果，它的核心方法是 `void run()`。
- 如果需要从异步任务拿到结果，后面要学习 `Callable + Future`。
- 章节代码统一放在 `src/main/java/chapterXX_章节名` 下面，目录名本身就是合法 Java 包名。

接下来仍然不要直接跳到线程池、AQS 或 JMM。优先理解：

1. 运行并观察 `ThreadCreationDemo`。
2. `Callable` / `Future` 解决了什么问题。
3. 线程生命周期和常见状态。

## 3. 推荐下一步

建议下一次学习时先做这几件事：

1. 先运行 `ThreadCreationDemo`，观察两个子线程和 `main` 线程的输出。
2. 再补充 `CallableFutureDemo`，专门解决“任务如何返回结果”的问题。
3. 每个 Demo 保持短小，只说明一个知识点。
4. 每完成一个知识点，就在根目录 `README.md` 和本文件里更新进度。

第一个推荐 Demo：

- 已完成：`ThreadCreationDemo`：演示继承 `Thread` 和实现 `Runnable` 的区别。
- 下一步：`CallableFutureDemo`：演示如何从异步任务中拿到返回值。
- 后续：`ThreadStateDemo`：观察 `NEW`、`RUNNABLE`、`TIMED_WAITING`、`TERMINATED` 等线程状态。

## 4. 和 AI 一起学习的风格

请 AI 按下面的方式和我协作：

- 用中文讲解，语气可以轻松一点，但不要跳步骤。
- 先问“这个知识点解决什么问题”，再写代码。
- 每次只推进一个小知识点，不要一次塞太多概念。
- 讲原理时要结合最小可运行 Demo。
- 代码要短，能复现现象即可，不追求复杂封装。
- 解释时多对比：为什么不用另一种写法，区别在哪里。
- 如果我概念混乱，优先用生活化类比帮我重新建立直觉。
- 不要只给结论，要引导我自己说出一部分理解。
- 如果需要改仓库，请直接改文件，并说明改了哪里。

## 5. 每个知识点的讨论模板

学习每个知识点时，建议按这个顺序：

1. 它解决了什么问题？
2. 最小代码怎么写？
3. 运行结果说明了什么？
4. 背后的机制是什么？
5. 它可能在哪些场景出错？
6. 它和相近方案有什么区别？
7. 用一句话总结它的本质。

## 6. 模块推进顺序

当前建议按这个顺序推进：

1. `chapter01_thread_foundation`：线程创建、任务模型、线程状态。
2. `chapter02_synchronization`：`synchronized`、`volatile`、`wait/notify`。
3. `chapter05_atomic_and_volatile`：原子类、CAS、`LongAdder`、`volatile` 深入。
4. `chapter03_lock_and_aqs`：`ReentrantLock`、`Condition`、AQS 基本思想。
5. `chapter04_concurrent_collections`：并发容器的使用场景和取舍。
6. `chapter06_thread_pool`：线程池参数、拒绝策略、任务调度。
7. `chapter07_producer_consumer`：线程协作模型。
8. `chapter08_jmm_and_happens_before`：JMM、可见性、有序性、happens-before。
9. `chapter09_performance_and_debug`：死锁、线程 dump、JFR、VisualVM。
10. `chapter10_practice_projects`：综合练习。

## 7. 给下一位 AI 的提醒

- 当前项目不是一个完整 Java 工程，而是学习仓库骨架。
- 不要假设已经有构建工具或源码目录，先检查项目状态。
- 如果要继续推进，优先补 `chapter01_thread_foundation` 示例。
- 每次新增 Demo，都要同时更新根目录 `README.md` 和本进度文件。
- 每次学习阶段结束，都要更新本文件的“我目前学到哪里”和“推荐下一步”。
- 保持示例代码简单、可运行、可解释。

